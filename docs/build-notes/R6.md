# R6 — Robustness Final Check and Verifier Submission

- Add a robustness battery over candidate walk-forward reports: deflated Sharpe, parameter sensitivity, subperiod stability, block bootstrap, and cost sensitivity.
- Submit best research candidates to the deterministic verifier with maker-checker separation.
- Record verifier results as registry audit metadata while preserving `status: candidate` and `max_notional_quote: 0` for rejected strategies.
- If any candidate is proposed for approval, stop for A-002 human approval instead of self-approving.
- Keep all outputs report-only; no live trading, no alert channels, no capital allocation changes.
