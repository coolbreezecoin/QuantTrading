# S13 — 监控、告警、账本、Fill-Fidelity

- S13 实现轻量 dashboard snapshot，不依赖外部 Grafana 服务。
- 告警 sink 默认 disabled，调用时只返回 `alert_disabled`，不发网络请求。
- trade ledger 与 fill-fidelity 均写 Parquet，保持后续分析友好。
- Fill-Fidelity 对比 expected vs actual price/qty，输出 slippage bps 与数量偏差。
- dashboard snapshot 汇总 data health、strategy、risk、fill fidelity、loop heartbeat。
- 新增校准文档，说明用实测 bps 回填 `config/fills.yaml` 的流程。
- S13 不发送 Telegram/Slack/Discord，不调用付费或外部告警 API。
- S13 不接 live broker，数据来源仍为本地 paper/报告对象。
