# F1 — Structural Data Layer

- Extend the market-data layer with research-only structural samples: funding rates, perp mark prices, spot prices, and derived basis.
- Use public ccxt-compatible endpoints only; no API keys, no private exchange methods, and no order endpoints.
- Keep production config untouched: perp symbols are function arguments in research/data code, not `config/exchanges.yaml` or `risk-policy.yaml` changes.
- Store funding and basis in DuckDB with idempotent primary keys so F2 can rerun reports reproducibly.
- Provide pure normalization/pagination helpers with fake-client tests; real collection can be retried and is safe to skip if a public venue blocks a route.
- Add quality reporting for funding cadence gaps, duplicate timestamps, basis coverage, and backfill capability notes.
- Document which fields are historical-backfillable (funding, mark/spot candles) and which remain forward-only (L2 orderbook depth).
- Preserve S4 forward funding compatibility by keeping `FundingRate.market_type == "perp_only"`.
