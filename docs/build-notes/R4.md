# R4 — Add At Most Two Conservative Strategy Families

- Add research-only `volatility_target_trend` long-only trend following with slower lookbacks, ATR stops, regime filter, and spacing.
- Add research-only `regime_switch_existing` that combines R3 robust momentum in trend regimes and robust mean reversion in chop regimes.
- Keep implementations deterministic and past-only; no ML, no factor pile, no cross-exchange assumptions.
- Evaluate each family with the same walk-forward setup, parameter trial count, diagnostics, and verifier thresholds.
- Write local reports marking pass/reject honestly; do not approve strategies or edit registry notional.
