# R2 — Diagnose Existing Strategy OOS Failure

- Add a research diagnostics module that consumes walk-forward reports and trade logs.
- Attribute each baseline strategy by OOS turnover, fee drag, regime PnL, win rate, average win/loss ratio, and signal lag.
- Keep the report factual: infer the dominant failure mode from metrics and emit reasons, not strategy approval.
- Reuse existing strategy SDK/backtest/walk-forward objects; do not change strategy parameters in R2.
- Generate one report each for `momentum_breakout` and `mean_reversion` using current configs.
- Keep all outputs local/ignored under `reports/`; no live trading, no registry approval, no external services.
