# S5 — 特征库

- 特征库保持纯函数：输入 `list[OhlcvBar]`，输出同长度序列。
- warmup 或不可计算位置返回 `None`，避免用隐式 0 污染策略。
- 实现收益率、滚动波动率、ATR、成交量变化、bar 内价差 bps。
- 所有函数先按时间排序，保证同输入同输出。
- lookahead 检测通过逐步截断输入，比较全量结果在同一 index 的值是否变化。
- 测试包含复现性、关键数值、以及故意泄漏未来 close 的用例。
- S5 不读取外部数据、不调用交易所、不产生交易信号。

