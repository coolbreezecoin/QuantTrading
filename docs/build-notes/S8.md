# S8 — Walk-forward 验证框架

- 新增滚动 IS/OOS window builder，按 bar index 明确 purge 与 embargo。
- OOS 起点 = IS 结束 + purge + embargo，避免相邻重叠信息泄漏。
- runner 分别对 IS 和 OOS 调用同一个 signal generator，再分别回测。
- OOS 验收只看 OOS 报告，IS 报告仅用于衰减/诊断。
- 参数试验次数记录在报告里，并折算为 deflated Sharpe 惩罚。
- OOS 段按收尾价格变化粗分 bull/bear/chop，供后续 verifier 看分行情表现。
- 测试覆盖 window 间隔、Sharpe 惩罚、IS→OOS 衰减和多 regime 可见。
- S8 不改策略参数、不批准策略、不执行交易。

