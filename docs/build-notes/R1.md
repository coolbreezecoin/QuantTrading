# R1 — Baselines and Beat Predicate

- Add `config/research.yaml` for research-stage benchmark and beat criteria, separate from risk policy.
- Model research config with pydantic and load it through `load_all_configs`.
- Implement buy-and-hold BTC and equal-weight BTC/ETH/SOL baselines using the same OHLCV history and fee model.
- Compute fee-adjusted total return, annualized return, Sharpe, max drawdown, and Calmar.
- Define `beats_benchmark` over OOS, fee-adjusted, risk-adjusted metrics: Calmar improvement or Sharpe non-inferiority with no worse drawdown.
- Keep R1 report-only: no strategy approval, no registry notional changes, no live execution, no external network calls.
- Generate local ignored reports only when historical DuckDB data is present.
